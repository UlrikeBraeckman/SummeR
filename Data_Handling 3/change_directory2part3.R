# ensure the directory that the student first is get to is the Data handling part 3 directory
# Thus, it is easy to naviagte to gain access to all the content and data needed for lesson
# Student will not be aware of this, this work automatically 

# if the current directory is outside Data_Handling_part_3 folder, 
# set directory to the parent.
# curdir <- basename(getwd())
# if(curdir != "Data_Handling 3") {
#   setwd("SummeR/Data_Handling 3/")
# }
# rm(curdir)